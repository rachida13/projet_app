import 'package:flutter/material.dart';

class SelectionneAvatr extends StatelessWidget {
  const SelectionneAvatr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [
            Text('you avatar'),
          ],
        ),
      ),
    );
  }
}
